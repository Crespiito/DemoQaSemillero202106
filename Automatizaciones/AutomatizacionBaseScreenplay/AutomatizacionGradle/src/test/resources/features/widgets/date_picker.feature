#language: es

  Característica: Como usuario de demoQA deseo comprobar el funcionamiento
    de los elementos del modulo widgets

  Escenario: como usuario del elemento Date Picker deseo comprobar el item select date
    Dado que como usuario del item selecte date
    Cuando selecciono una fecha del select
    Entonces verifico que el item guarda la fecha que elijo

    Escenario: como usuario del elemento Date Picker deseo comprobar el item Date and Time
      Dado que como usuario del item selecte date
      Cuando selecciono una fecha del select
      Entonces verifico que el item guarda la fecha que elijo
